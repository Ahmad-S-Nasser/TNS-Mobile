import 'package:get_it/get_it.dart';

final sl = GetIt.instance;

Future<void> init() async {
  // Add registrations here as features are implemented
  // e.g. sl.registerFactory(() => AuthCubit(sl()));
}
